'use strict';
<% if (tableAccess) { %>
const forumTable = require('../forum_table');
<% } %>
exports.handler = (event, context, callback) => {
  callback(null, {
    statusCode: 200,
    headers: {},
    body: ''
  });
};
