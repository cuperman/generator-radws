'use strict';
<% if (tableAccess) { %>
const <%= tableVariableName %> = require('../<%= tableFileBaseName %>');
<% } %>
exports.handler = (event, context, callback) => {
  callback(null, {
    statusCode: 200,
    headers: {},
    body: JSON.stringify({ handler: '<%= handler %>' })
  });
};
